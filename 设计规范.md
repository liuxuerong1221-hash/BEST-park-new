# BEST园区可视化集成平台设计规范

## 1. 设计定位

本项目为园区集成可视化管理大屏，视觉方向为深色科技风、半透明信息浮层、蓝青色高亮数据、地图/建筑模型作为主视觉中心。

设计目标：

- 首屏优先展示可运营数据，不做营销式首页。
- 信息密度高但层级清晰，适合大屏远距离浏览。
- 左右面板承载统计和列表，中间区域保留给建筑模型、地图点位、浮动提示和楼层控件。
- 所有页面保持统一的头部、底部导航、左右边框装饰、背景图和面板语言。

当前设计基线页面：

- 园区总览：`HomeView.vue`
- 会议室看板：`MeetingRoomView.vue`
- 通行看板：`AccessView.vue`

---

## 2. 画布与全局布局

### 画布尺寸

| 项 | 规格 |
|----|------|
| 设计基准 | `1920 x 1080` |
| 缩放方式 | `App.vue` 按窗口等比缩放整个画布 |
| 页面根尺寸 | `$screen-width: 1920px`，`$screen-height: 1080px` |
| 背景色 | `$color-bg: #12233A` |
| 背景图 | `src/assets/images/bg图@2x.png`，覆盖全屏 |

页面不要单独做移动端响应式。新增页面应按 1920x1080 画布完成，再由全局缩放适配窗口。

### 页面骨架

标准页面采用三栏结构：

```text
顶部 Header: 78px
主内容区: 左栏 392px / 中间自适应 / 右栏 390px
底部导航: 67px
页面左右内边距: 40px
栏间距: 8px
```

对应 token：

- `$panel-left-w: 392px`
- `$panel-right-w: 390px`
- `$header-h: 78px`
- `$bottom-nav-h: 67px`

### 左右边框装饰

- 使用 `left-左.png`、`left-右.png`。
- 位置：左右贴边，`top: 8px`，`bottom: 8px`。
- 层级：`z-index: 30`，高于页面内容。
- 必须 `pointer-events: none`，不能影响页面交互。

---

## 3. 色彩规范

### 基础色

| Token | 色值 | 用途 |
|-------|------|------|
| `$color-bg` | `#12233A` | 页面主背景 |
| `$color-bg-panel` | `rgba(10, 29, 53, 0.6)` | 标准面板背景 |
| `$color-bg-overlay` | `rgba(0, 30, 58, 0.5)` | 深色遮罩、弹层底 |

### 品牌与高亮色

| Token | 色值 | 用途 |
|-------|------|------|
| `$color-primary` | `#00AEFF` | 主蓝、标题装饰、重点状态 |
| `$color-primary-bright` | `#4DF2FF` | 亮青蓝、高亮数字、图标轨道 |
| `$color-primary-glow` | `rgba(0, 174, 255, 0.4)` | 蓝色发光 |
| `$color-primary-dim` | `rgba(0, 174, 255, 0.06)` | 淡蓝底 |

### 辅助与状态色

| Token | 色值 | 用途 |
|-------|------|------|
| `$color-accent` | `#EDD505` | 黄色数值、用水/占用类强调 |
| `$color-accent-light` | `#FFE92B` | 亮黄轨道、局部高亮 |
| `$color-success` | `#2ED0B0` | 正常、在线、空闲、成功 |
| `$color-warning` | `#F9B933` | 告警、提醒、警示趋势 |
| `$color-error` | `#D82F29` | 异常、离线、错误 |

### 文本色

| Token | 色值 | 用途 |
|-------|------|------|
| `$color-text-1` | `#FFFFFF` | 主文本 |
| `$color-text-2` | `#D1EDFF` | 标签、说明、次要文本 |
| `$color-text-3` | `#8AFFF8` | 点缀文本、设备统计标签 |
| `$color-text-num` | `#4EF4FF` | 数字高亮 |

### 使用原则

- 页面主视觉以深蓝底、青蓝高亮为主，不要新增大面积紫色、橙色、米色主题。
- 黄色只用于对比型指标、告警或水/占用等特定强调，避免同时出现多个无意义黄色高亮。
- 红色仅用于异常、离线、告警紧急状态，不用于普通强调。
- 新增颜色先判断是否能复用现有 token；高复用颜色再加入 `variables.scss`。

---

## 4. 字体与数字

### 字体族

| Token | 用途 |
|-------|------|
| `$font-title` | 大标题装饰字 |
| `$font-number` | 数字、统计值 |
| `$font-body` | 正文、标签、按钮 |

### 字号

| Token | 值 | 用途 |
|-------|----|------|
| `$font-size-xxl` | `46px` | 大屏主标题 |
| `$font-size-xl` | `30px` | 大号数字 |
| `$font-size-lg` | `24px` | 中号数字 |
| `$font-size-md` | `20px` | 面板内核心值 |
| `$font-size-sm` | `16px` | 正文 |
| `$font-size-xs` | `14px` | 辅助文字、按钮 |
| `$font-size-xxs` | `12px` | 标签、单位、图例 |

### 数字规范

- 统计数字统一使用 `$font-number`。
- 数字行高默认 `1`，避免上下视觉漂移。
- 大屏指标优先用高亮色和轻微发光，而不是加粗描边。
- 单位字号小于数字，常用 `12px` 或 `10px`。
- 空数据统一显示 `-`，不要隐藏字段。

---

## 5. 间距、圆角与边框

### 间距 token

| Token | 值 |
|-------|----|
| `$spacing-xs` | `4px` |
| `$spacing-sm` | `8px` |
| `$spacing-md` | `16px` |
| `$spacing-lg` | `24px` |
| `$spacing-xl` | `32px` |

### 圆角

| Token | 值 | 用途 |
|-------|----|------|
| `$radius-sm` | `4px` | 小卡片、分段控件 |
| `$radius-md` | `8px` | 标准面板 |
| `$radius-lg` | `12px` | 大浮层或特殊容器 |

当前设计中卡片和面板偏硬朗，除圆形图标、圆环、返回按钮外，不要随意使用大圆角胶囊。

### 线条

| Token | 值 | 用途 |
|-------|----|------|
| `$color-border` | `rgba(0, 174, 255, 0.3)` | 标准面板边框 |
| `$color-line` | `rgba(0, 174, 255, 0.2)` | 分隔线 |
| `$color-line-bright` | `#00AEFF` | 高亮线条 |

---

## 6. 标准组件规范

### BasePanel

所有标准数据面板优先使用 `BasePanel`。

视觉规格：

- 背景：`$color-bg-panel`
- 边框：`1px solid $color-border`
- 圆角：`$radius-md`
- 内边距：`$spacing-sm $spacing-md`
- 布局：纵向 flex
- 溢出：`hidden`

用法：

```vue
<BasePanel class="energy-stats" title="能耗分析">
  <div class="energy-stats__body">...</div>
</BasePanel>
```

带右侧操作区：

```vue
<BasePanel class="energy-chart" title="本年度能耗分析">
  <template #actions>
    <div class="energy-chart__legend">...</div>
  </template>
  <v-chart ... />
</BasePanel>
```

约束：

- 不要在业务面板重复写面板背景、边框、圆角、标题栏。
- 业务面板可覆盖 `gap`、`flex`、`min-height`，但不应覆盖标准面板视觉。

### PanelTitle

面板标题由 `PanelTitle` 承担，通常通过 `BasePanel` 间接使用。

视觉规格：

- 高度：`32px`
- 左侧主色边：`3px solid $color-primary`
- 背景：深蓝向透明渐变
- 标题左内距：`10px`
- 标题文字：白到浅蓝渐变，`16px`，`font-weight: 600`

标题文本必须短且直指业务对象，如“能耗分析”“车位统计”“通行记录”。不要写功能说明句。

### MetricBlock

用于“标签 + 数值 + 单位”的指标组合。

推荐场景：

- 能耗：`unit-placement="label"`，单位放小标题后。
- 车位：`unit-placement="below"`，数值在上、标签在下，`gap="8px"`。
- 圆环中心：`unit-placement="below"`，人数和单位上下结构，默认紧凑间距。

色彩语义：

| tone | 用途 |
|------|------|
| `primary` | 主蓝强调 |
| `bright` | 亮青蓝核心数据 |
| `accent` | 黄色对比数据 |
| `success` | 空闲、在线、正常 |
| `warning` | 告警、警示 |

### AnimatedIconFrame

用于带动效的图标。

| effect | 当前用途 | 尺寸 |
|--------|----------|------|
| `orbit` | 能耗图标轨道追逐光点 | `52px` |
| `scan` | 车位图标球体扫描线 | `58px` |
| `none` | 仅展示图标 | 按场景 |

动效规则：

- 轨道动效使用两颗发光粒子，前大后小，沿椭圆轨道追逐。
- 扫描线高度为 `1px`，范围限制在图标球体内，带淡入淡出。
- 所有动效必须支持 `prefers-reduced-motion: reduce`。

### SegmentedTabs

用于小型分段切换。

视觉规格：

- 单项默认宽度：`46px`
- 高度：`24px`
- 边框：蓝色半透明
- 激活态：蓝色半透明填充 + 内发光

适用于“今日/近7天”“全部/使用中/空闲中”等短文本切换。长文本不应挤入分段控件，应改用下拉或列表筛选。

---

## 7. 页面模式

### 园区总览页

布局特征：

- 左栏：能耗、年度能耗图、环境趋势、告警趋势。
- 中间：环境统计条、地图/建筑模型留白、指南针、楼层选择器、浮动入口。
- 右栏：人员通行、车位统计、设备运营统计。

设计要求：

- 首页不要放复杂表单。
- 左右栏面板间距保持 `10px`。
- 中间区域保持通透，避免用大卡片遮挡地图主视觉。
- 设备连线、图标动效等装饰应服务于数据关系，不要遮挡数字。

### 会议室页

布局特征：

- 左栏：会议室统计、会议室列表。
- 中间：地图点位、返回按钮、指南针、楼层选择器。
- 右栏：摄像头/会议室详情。

设计要求：

- 返回按钮位置固定在左栏右侧、地图上方，尺寸 `105 x 40`。
- 地图点位选中态要与列表选中态同步。
- 摄像头面板属于高优先级内容，信息层级应高于普通列表。

### 通行页

布局特征：

- 左栏：车辆出入口监控、人员出入趋势、车辆出入趋势。
- 中间：顶部通行统计条、通行浮动提示、地图控件。
- 右栏：访客统计、车位概况、通行记录。

设计要求：

- 通行页左右栏顶部 padding 为 `14px`，比总览页更靠上，以容纳顶部统计条。
- 通行记录是持续更新信息，列表密度可高于普通卡片。
- 视频、抓拍图、车牌、人名等内容要预留空数据和加载失败状态。

---

## 8. 图表规范

图表统一使用 ECharts + `vue-echarts`。

容器规则：

- 图表外壳使用 `BasePanel`。
- 图例、筛选器放入 `actions` slot。
- 图表主体使用 `<v-chart class="xxx__chart" :option="option" autoresize />`。
- 图表区域必须有 `min-height`，避免被 flex 压缩到不可见。

坐标与文字：

- 坐标轴文字使用 `$color-text-2`，字号 `10px - 12px`。
- 网格线使用蓝色低透明度虚线。
- tooltip 背景使用半透明深蓝，文字使用浅蓝白。
- 单位要出现在轴名、标题辅助信息或 tooltip 中，不能只存在需求文档里。

颜色：

- 用电：蓝青色系。
- 用水：绿色或黄色对比色，按当前页面上下文保持一致。
- 告警：黄色或红色。
- 进入/离开、预约/到访等双线图必须使用可区分颜色。

---

## 9. 交互与动效

### 基础交互

- 可点击元素必须有 hover 或 active 状态。
- 键盘可达按钮应保留 `focus-visible` 反馈。
- 危险或远程控制操作必须二次确认。
- 图片、视频、监控画面支持放大查看。

### 地图与点位

- 地图点位 hover 显示名称。
- 点击点位后聚焦当前位置，并联动列表选中。
- 楼层切换通过 `FloorSelector` 完成。
- `Compass` 固定在地图控件组中，和楼层选择器垂直排列，间距 `8px`。

### 动效原则

- 动效优先使用 `transform` 和 `opacity`。
- 避免大面积背景闪烁。
- 循环动效应低干扰，不影响读数。
- 扫描、粒子、发光用于强调设备状态或图标，不用于普通文本。
- 必须提供 `prefers-reduced-motion` 降级。

---

## 10. 数据状态与异常态

| 状态 | 展示规则 |
|------|----------|
| 空数据 | 显示 `-`，保留结构 |
| 加载失败 | 保留面板，局部显示缺省或错误状态 |
| 图片缺失 | 使用缺省图或占位区域，不压塌布局 |
| 视频无流 | 显示无信号/暂无视频状态 |
| 离线设备 | 使用 `$color-error` 或低亮度状态样式 |
| 在线/正常 | 使用 `$color-success` |

图表没有数据时仍保留坐标轴和标题，数据点或柱子显示为空，不隐藏整个面板。

---

## 11. 文案规范

- 面板标题使用名词短语：如“人员通行”“车位概况”“告警趋势”。
- 标签使用业务字段名：如“今日进入人次”“车位总数”“在线”。
- 单位使用括号或紧跟数值，保持同一面板一致。
- 数值中的单位不要和标题单位重复出现。
- 按钮文案保持短：如“返回”“关闭”“全屏”“控制”。
- 空值统一为 `-`，不要使用“暂无”“无数据”等多种写法混用。

---

## 12. 资产规范

图片资源优先放入 `src/assets/images/`，可复用 Vue SVG 图标放入 `src/components/icons/`。

使用规则：

- 背景、边框装饰、复杂设计图标使用设计导出的 PNG/SVG。
- 简单线条、发光、边框、扫描线优先用 CSS 或内联 SVG。
- 资源命名应语义化，避免继续使用临时 hash 或 D2C 导出名。
- 图片必须设置 `alt=""` 或有意义的 `alt`，装饰图统一空 alt。

---

## 13. 新增页面/组件检查清单

新增或调整页面前检查：

- 是否复用全局 1920x1080 画布与三栏布局。
- 是否复用 `AppHeader`、`BottomNav`、左右边框和背景图。
- 标准面板是否使用 `BasePanel`。
- 指标是否优先使用 `MetricBlock`。
- 动效图标是否优先使用 `AnimatedIconFrame`。
- 图表是否使用 `BasePanel + v-chart + autoresize`。
- 是否定义空数据、加载失败、图片/视频缺失状态。
- 是否避免文字溢出和图表遮挡。
- 是否检查 `#/`、`#/meeting-room`、`#/access` 视觉一致性。

---

## 14. 禁止项

- 禁止在页面内重新发明标准面板样式。
- 禁止用大面积纯色卡片覆盖地图主视觉。
- 禁止新增无语义的大渐变背景、装饰光球、无功能粒子。
- 禁止让标题、图例、筛选器覆盖图表。
- 禁止一个页面使用与其他页面不同的头部、底部或左右边框规则。
- 禁止将一次性业务卡片抽象成过度通用组件。
- 禁止在当前深色科技风中引入大面积暖色、浅色或营销化版式。
