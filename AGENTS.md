# BEST园区可视化集成平台 — 协作开发指南

## 项目现状

这是一个 Vue 3 + Vite 的园区大屏项目，当前已形成 3 个页面入口：

| 路由 | 页面 | 主要职责 |
|------|------|----------|
| `#/` | `HomeView.vue` | 园区总览：首页能耗、环境、人员、车位、设备运营 |
| `#/meeting-room` | `MeetingRoomView.vue` | 会议室看板：会议室统计、列表、地图点位、摄像头面板 |
| `#/access` | `AccessView.vue` | 通行看板：车辆/人员趋势、访客、车位概况、通行记录 |

全局画布仍按 `1920 x 1080` 设计稿基准实现，由 `App.vue` 统一缩放。页面开发默认在这个固定设计画布内完成，不单独做移动端响应式。

---

## 技术栈与命令

| 层级 | 技术 | 协作约定 |
|------|------|----------|
| 框架 | Vue 3 + `<script setup>` | 新代码使用 Composition API |
| 语言 | TypeScript | 新增公共接口必须显式定义类型 |
| 构建 | Vite 5 | 路径别名 `@` 指向 `src/` |
| 样式 | SCSS + scoped | 样式默认写在组件内，公共 token 在 `variables.scss` |
| 图表 | ECharts + vue-echarts | 图表组件放 `components/charts/`，按需注册模块 |
| UI 库 | Element Plus | 如使用 EP 组件，走现有自动按需导入 |

常用命令：

```bash
npm run dev
npm run build
```

构建中出现 Sass legacy API、chunk size 警告属于当前已知问题；只要 `vue-tsc` 和 Vite build 退出码为 0，即视为通过基础验证。

---

## 目录边界

```text
src/
├── views/              # 页面编排，只负责布局、页面状态和路由级交互
├── components/
│   ├── common/         # 跨页面可复用基础组件
│   ├── panels/         # 业务面板，承载页面局部业务 UI
│   ├── charts/         # ECharts 图表组件
│   └── icons/          # 可复用 SVG/Vue 图标
├── styles/
│   ├── variables.scss  # 全局设计 token
│   └── global.scss     # 全局样式
├── router/             # 路由入口
├── api/                # 请求封装
├── stores/             # Pinia 状态
└── assets/             # 图片、设计导出 SVG/PNG
```

分工时优先按页面或面板拆分：

- 页面负责人只改对应 `views/*View.vue` 和该页面独占面板。
- 面板负责人只改 `components/panels/` 下约定文件。
- 图表负责人只改 `components/charts/` 和图表数据配置。
- 公共组件负责人改 `components/common/`，必须同步检查所有引用页面。
- 不要在同一轮多人同时改 `App.vue`、`variables.scss`、`BasePanel.vue`、`PanelTitle.vue`，这些文件影响面大，需要先约定。

---

## 公共组件使用规则

### `BasePanel`

所有标准面板优先使用 `BasePanel`，不要重复写面板背景、边框、圆角、padding、标题栏。

```vue
<BasePanel class="energy-stats" title="能耗分析">
  <div class="energy-stats__body">...</div>
</BasePanel>
```

支持：

- `title: string`
- 默认 slot：面板内容
- `actions` slot：标题右侧图例、筛选器、按钮

业务组件可以通过传入的根 class 覆盖 `gap`、`flex`、`min-height` 等布局属性，但不要覆盖基础面板视觉，除非设计稿明确要求。

### `MetricBlock`

统一处理“标签 + 数值 + 单位”的展示，避免各面板重复写数字字体、色彩和单位位置。

核心 props：

- `label`
- `value`
- `unit`
- `unitPlacement: 'label' | 'value' | 'below'`
- `tone: 'primary' | 'bright' | 'accent' | 'success' | 'warning'`
- `size: 'sm' | 'md' | 'lg'`
- `align: 'start' | 'center' | 'end'`
- `gap?: string`

使用约定：

- 能耗类：单位放标题后，使用 `unit-placement="label"`，如 `本年度耗电量 (kWh)`。
- 车位类：数值在上、标签在下，使用 `unit-placement="below"`，并按视觉需要传 `gap="8px"`。
- 圆环中心类：人数和单位上下结构，使用 `unit-placement="below"`，默认 `gap` 保持紧凑。

### `AnimatedIconFrame`

统一处理带动效的图标框。

核心 props：

- `src`
- `alt?`
- `size?: number`
- `effect: 'orbit' | 'scan' | 'none'`
- `tone: 'cyan' | 'yellow'`

使用约定：

- 能耗图标：`effect="orbit"`，`size="52"`，保留椭圆轨道和两颗追逐光点。
- 车位图标：`effect="scan"`，`size="58"`，保留 1px 扫描线和球体内淡入淡出下扫。
- 新增动画必须优先用 `transform` / `opacity`，并补充 `prefers-reduced-motion`。

---

## 页面与面板开发规则

### 页面 `views`

页面只负责整体布局和跨组件状态，不写复杂业务卡片细节。

- 首页、会议室、通行页都沿用左栏 / 中间地图 / 右栏结构。
- 地图区控件优先复用 `Compass`、`FloorSelector`、点位或浮层组件。
- 返回按钮、浮动入口等跨页面可复用控件应沉淀到 `common/`，不要散落在多个页面里复制。

### 面板 `panels`

业务面板负责局部 UI 和本面板内部状态。

- 根节点优先是 `<BasePanel class="xxx" title="...">`。
- class 使用 BEM：`block__element--modifier`。
- 面板内专属结构可以保留在本文件，不要为了复用而抽取一次性结构。
- 涉及 DOM 测量的面板，例如设备连线，必须使用 `ref + nextTick + ResizeObserver`，并在卸载时清理监听。

### 图表 `charts`

图表组件只负责 ECharts option 和图表容器。

- 使用 `<v-chart :option="option" autoresize />`。
- option 使用 `computed`。
- 按需注册 ECharts 模块，禁止全量 `import * as echarts from 'echarts'`。
- 标题、图例、筛选器走 `BasePanel` 的 `actions` slot。

---

## 样式与设计还原

- 全局语义色、字体、间距优先使用 `src/styles/variables.scss`。
- 设计稿里的特定像素尺寸可以保留在组件内，例如图标 52px、扫描线 1px、卡片 87px 高度。
- 禁止把业务专属的渐变、点位坐标、图表数据 token 化。
- 新增颜色前先判断是否已有语义 token；高复用颜色再加入 `variables.scss`。
- 页面整体背景、左右边框、顶部头部、底部导航保持跨页面一致。
- 修改公共视觉组件后，必须检查所有路由：`#/`、`#/meeting-room`、`#/access`。

---

## 资产与设计稿

- MasterGo/D2C 生成的图片、SVG、DSL 临时文件不要直接当作最终规范；最终以 `src/assets/` 和组件代码为准。
- 复杂图标和背景图优先使用设计导出的 SVG/PNG。
- 简单装饰、边框、光效优先用 CSS/SVG 实现，避免无意义切图。
- 新增资源放入语义明确的位置，避免继续向根目录散落临时资源。

---

## 需求文档补充约束

依据 `需求功能规格说明书_BEST园区集成可视化管理平台_best.docx` V1.0，后续开发除设计稿外还需满足以下需求约束。

### 全局交互与数据规则

- 大屏分辨率基准为 `1920 x 1080`，页面左右报表以半透明浮层固定在界面左右，建筑模型/地图区域覆盖中间主视觉。
- 顶部区域需要预留登录信息和天气信息展示能力。
- 数据为空、接口无值、统计项暂无数据时统一显示 `-`；统计图表仍保留结构，不因空数据隐藏整个模块。
- 实时监控、设备在线/故障等实时数据按 3 秒刷新；小时颗粒度数据每小时刷新；其他低频统计默认每日刷新。
- 报表存在切换按钮时，应支持手动切换和自动轮播；综合态势中能耗图表按 5 秒切换，趋势类/监控类常见轮播按 30 秒切换。
- 当统计类型过多且无法折叠时，必须支持横向滑动或轮播展示，不能让内容溢出卡片。
- 所有图片、抓拍图、视频流、监控画面应支持点击放大/缩小；远程控制、删除、手动处理等高风险操作必须二次确认。
- 建模/地图点位交互应支持列表联动、点位 hover 显示名称、点击聚焦位置、左侧列表同步选中、返回上一层级。

### 模块范围

需求文档定义 4 大模块，当前代码已覆盖其中一部分，后续新增页面或面板需按该模块拆分：

- 综合态势：能耗分析、环境质量趋势、本年度告警趋势、人员通行、车位统计、设备运营统计、会议室、第一人称漫游。
- 通行态势：车辆出入口监控、人员出入趋势、车辆出入趋势、访客到访统计、车位概况、通行记录、顶部进出数据、门禁/摆闸/道闸详情。
- 安防态势：监控设备统计、重点区域监控、安防告警处理统计、告警趋势、告警列表、告警产生、告警详情、摄像头、轨迹查询、机房和机柜设备管理。
- 设备能耗：水电能耗数据对比、用水/用电能耗趋势、今日能耗、照明监测、空调新风监测、电表、水表、广播、信息发布屏、楼宇设备。

### 当前页面自查重点

- 综合态势首页：
  - 能耗分析需展示本年度耗电量 `kWh`、本年度耗水量 `t`，年度图表按 1 月至当前月展示，tooltip 显示月份和数值。
  - 环境趋势来源为空气质量传感器，类型包括 CO2、CO、温度、湿度、PM2.5，X 轴为今日 0 点至当前时间，颗粒度 1 小时。
  - 告警趋势展示本年度 1 月至当前月，单位为条。
  - 人员通行统计今日 0 点至当前进入/离开人次。
  - 车位统计展示总数、已使用、空闲中，单位为个。
  - 设备运营统计展示摄像头、通行、广播、信息发布屏、楼宇、能耗等设备类型的总数和在线数量。
- 会议室页：
  - 会议室入口应联动地图/建模点位和会议室列表。
  - 会议室列表支持关键词搜索、状态筛选、分页；状态来源按会议系统主机在线/离线映射为使用中/空闲中。
  - 点击列表或点位后，右侧展示实时监控、会议室名称、容纳人数、位置和当前状态，监控支持放大。
- 通行页：
  - 车辆出入口监控支持监控位置切换、视频轮播开关、30 秒自动切换和全屏放大。
  - 人员/车辆出入趋势默认展示今日，30 秒后切换近 7 天并循环；tooltip 需同时展示进入和离开数。
  - 访客统计展示今日预约访客、今日到访访客和近 7 天预约/到访趋势。
  - 通行记录支持车辆/人员记录、近 30 天日期筛选、分页或下滑加载，时间精确到秒。
  - 门禁、摆闸、道闸设备详情遵循“左侧列表 + 中间点位 + 右侧详情”的交互模式。
- 安防态势和设备能耗：
  - 当前若未实现完整页面，新增开发时不得复用综合态势标题或数据口径替代，应按需求文档单独建页面、面板和字段。
  - 安防告警类列表默认按发生时间倒序，支持状态/日期筛选；告警处理类操作需要二次确认。
  - 电表、水表、广播、信息发布屏、楼宇设备详情均需支持列表筛选、点位联动、右侧详情和楼栋聚合点位切层。

## 协作流程

每个任务开始前先确认所属范围：

1. 页面编排：改 `views/`，尽量不碰公共组件。
2. 独立面板：改单个 `panels/` 文件，必要时新增局部子组件。
3. 图表任务：改 `charts/`，保持面板外壳不变。
4. 公共能力：改 `common/`，必须说明会影响哪些页面。
5. Token/布局基线：改 `variables.scss`、`App.vue` 或路由结构，需要单独协调。

交付说明至少包含：

- 改了哪些区域。
- 是否影响公共组件。
- 是否跑过 `npm run build`。
- 哪些路由需要人工视觉复核。

---

## 验收清单

提交或交接前至少检查：

- `npm run build` 通过。
- 当前任务涉及的路由能打开，控制台无 Vue/DOM/资源加载报错。
- 面板标题、边框、背景、padding 与现有风格一致。
- 图标动效不超出预期范围，动画在缩放后不明显错位。
- 图表不遮挡标题、图例、筛选器。
- 文字不溢出按钮、卡片或统计项。
- 没有引入新的重复面板外壳、重复数字样式、重复图标动效。

---

## 禁止事项

- 禁止新增 Options API 组件。
- 禁止手改 `src/types/components.d.ts` 这类自动生成文件；需要时通过构建生成。
- 禁止复制 `BasePanel`、`MetricBlock`、`AnimatedIconFrame` 已覆盖的基础结构。
- 禁止无说明修改 `App.vue` 的缩放逻辑和 `$screen-width/$screen-height`。
- 禁止全量引入 ECharts。
- 禁止为单个业务卡片创建过度抽象的“万能组件”。
- 禁止删除或覆盖他人正在开发的页面/面板代码。

---

## 当前基线

- 已完成基础页面：园区总览、会议室、通行。
- 已沉淀公共组件：`BasePanel`、`PanelTitle`、`MetricBlock`、`AnimatedIconFrame`、`SegmentedTabs`、地图点位/浮层类组件。
- 首页能耗、车位、人员、设备面板已按公共组件整理。
- 后续新增页面或面板默认基于现有公共组件扩展，而不是从空白样式重新实现。
